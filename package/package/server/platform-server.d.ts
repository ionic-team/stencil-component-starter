import { BuildConfig, BuildContext, Diagnostic, PlatformApi } from '../util/interfaces';
export declare function createPlatformServer(config: BuildConfig, win: any, doc: any, diagnostics: Diagnostic[], isPrerender: boolean, ctx?: BuildContext): PlatformApi;
