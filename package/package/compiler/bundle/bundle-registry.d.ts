import { ComponentRegistry, Manifest, ModuleResults, StylesResults } from '../../util/interfaces';
export declare function generateComponentRegistry(manifest: Manifest, styleResults: StylesResults, moduleResults: ModuleResults): ComponentRegistry;
