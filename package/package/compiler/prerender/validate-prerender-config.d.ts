import { BuildConfig, PrerenderConfig } from '../../util/interfaces';
export declare function validatePrerenderConfig(config: BuildConfig): void;
export declare const DEFAULT_PRERENDER_CONFIG: PrerenderConfig;
